# Build artifacts
/bin/
/tmp/
*.exe
*.test


# Binary that "go build ./..." drops in the repository root
/alexandria

# Coverage and profiles
coverage.out
coverage.html
*.prof

# Logs
*.log

# The project's local certificate authority: a signing key, and a root that a
# machine has been told to trust. Committing it would hand anyone who clones the
# repository the ability to impersonate any site on every machine that trusts it.
/.certs/

# Helm subcharts, fetched by `helm dependency update`. Chart.lock is committed —
# it pins what was fetched — but the archives themselves are downloads.
/deploy/k8s/charts/*/charts/

# Secrets and local configuration
.env
.env.*
!.env.example

# OS and editors
.DS_Store
.idea/
.vscode/

# Internal notes: working documents, not part of the published repository
/docs/internal/
/docs/artifacts/
CURRENT_NOTES.md
/CLAUDE.md
/GEMINI.md

# Documentation site (Fumadocs)
/docs/node_modules/
/docs/.next/
/docs/out/
/docs/.source/

